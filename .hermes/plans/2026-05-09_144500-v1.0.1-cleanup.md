# paper-compass v1.0.1 规范化 & 清理计划

**创建时间**: 2026-05-09 14:45
**当前版本**: 2.0.0 → **目标版本**: 1.0.1
**分支**: main

---

## 一、概述

六项改进，聚焦项目公开化就绪：中文化 README、去 Mimo 特定命名、清理多余 badge、优化介绍文案、补充 Agent 部署文档、修正版本号。

## 二、任务拆解

### Task 1 — README 中文化（首要语言）

**目标**: README 用中文作为主要语言，同时提供英文版 `README.en.md`。

**实施**:
- `README.md` — 重写为中文（标题、介绍、安装、配置、使用方法、架构等全部中文化）
- `README.en.md` — 将当前英文 README 移入，作为英文版本
- 保留英文 README 的内容结构，翻译为流畅的中文

**涉及文件**:
- `README.md` ← 重写（中文）
- `README.en.md` ← 新建（英文版）

---

### Task 2 — 变量名去 Mimo 化

**目标**: 将 `MIMO_BASE_URL` / `XIAOMI_API_KEY` 替换为更通用的 `LLM_BASE_URL` / `LLM_API_KEY`。Model 名称 `mimo-v2.5-pro` 保持不变（它确实是模型名）。

**涉及文件（共 10+ 个）**:

| 文件 | 当前 | 改为 |
|------|------|------|
| `.env.example` | `MIMO_BASE_URL=...` / `XIAOMI_API_KEY=...` | `LLM_BASE_URL=...` / `LLM_API_KEY=...` |
| `configs/providers.yaml` | `base_url_env: MIMO_BASE_URL` / `api_key_env: XIAOMI_API_KEY` | `base_url_env: LLM_BASE_URL` / `api_key_env: LLM_API_KEY` |
| `scripts/healthcheck.py` L38 | `"MIMO_BASE_URL", "XIAOMI_API_KEY"` | `"LLM_BASE_URL", "LLM_API_KEY"` |
| `tests/test_env_utils.py` | `MIMO_BASE_URL` (4处) | `LLM_BASE_URL` |
| `README.md` | MIMO_BASE_URL/XIAOMI_API_KEY (8处) | LLM_BASE_URL/LLM_API_KEY |
| `README.en.md` | 同上 | 同上 |
| `docs/api-integration-notes.md` | Mimo / MIMO_BASE_URL / XIAOMI_API_KEY (多处) | 改用通用 LLM 表述 |
| `docs/deployment-test-report-2026-05-09.md` | MIMO_BASE_URL | LLM_BASE_URL |
| `docs/plans/02-provider-layering-plan.md` | MIMO_BASE_URL/XIAOMI_API_KEY | LLM_BASE_URL/LLM_API_KEY |
| `.hermes/plans/2026-05-09_112400-v2-standardization-plan.md` | MIMO_BASE_URL/XIAOMI_API_KEY (多处) | LLM_BASE_URL/LLM_API_KEY |
| 本地 `~/.hermes/profiles/papercompass/skills/research/paper-compass-dev/SKILL.md` | `MIMO_BASE_URL`, `XIAOMI_API_KEY` (prereqs + body) | `LLM_BASE_URL`, `LLM_API_KEY` |
| 本地 `.env` (gitignored, 手动改) | `MIMO_BASE_URL=...` / `XIAOMI_API_KEY=...` | `LLM_BASE_URL=...` / `LLM_API_KEY=...` |

**注意事项**:
- `mimo-v2.5-pro`（模型名称）不做修改 — 它确实是模型名
- `Mimo` 在注释/doc 中的出现改为 "LLM" 或 "LLM 服务"
- `.env` 是 gitignored，需要单独在本地修改

---

### Task 3 — 删除 "test 66 pass" badge

**目标**: README 顶部 badges 中 `tests-66/66 passing` badge 多余，删除。

**实施**:
- `README.md` L9: 删除 `[![Tests](https://img.shields.io/badge/tests-66%2F66%20passing-brightgreen.svg)](tests/)`
- `README.en.md`: 同样删除

---

### Task 4 — 项目介绍优化

**目标**: 优化 README 中的项目介绍文案，使其更清晰专业。

**方向**:
- 一句话定位：paper-compass 是什么
- 解决什么问题：Zotero 论文库 → AI Agent 可查询的知识基础设施
- 核心能力链条：三个关键词（PDF→Wiki、向量检索、MCP 集成）
- 与中国学术生态的关联（支持中文检索优化）

---

### Task 5 — 补充 Agent 部署提示词

**目标**: 为其他 AI Agent（如 Claude Code、Codex、Hermes 自身）提供部署 paper-compass 所需的上下文和指令。

**新建文件**: `AGENTS.md`

**内容结构**:
1. 项目简介（一句话 + 核心 pipeline）
2. 环境依赖（Python 3.11+, pip）
3. 配置步骤（.env.example → .env，填写 API key）
4. 数据准备（Zotero library → sync → index → wiki）
5. 启动 MCP 服务
6. MCP client 配置示例（Hermes config.yaml / Claude Desktop）
7. 常见问题 & 陷阱（WSL、BM25、Volcengine 格式等）
8. 验证方式（healthcheck、eval）

---

### Task 6 — 版本号修正: 2.0.0 → 1.0.1

**目标**: 版本不合理地从 1.5 跳到 2.0.0，应改为 1.0.0 起始 → 本次改动后为 1.0.1。

**涉及文件**:
| 文件 | 改动 |
|------|------|
| `pyproject.toml` L3 | `version = "2.0.0"` → `version = "1.0.1"` |
| `README.md` badged L10 | `version-2.0.0` → `version-1.0.1` |
| `README.en.md` | 同上 |

---

## 三、执行顺序

```
Task 6 (版本号) → 先改完版本号再改 README，README 中 badge 引用新版本号
Task 2 (变量重命名) → 全局搜索替换，注意不遗漏
Task 3 (删除 badge)
Task 4 (介绍优化) + Task 1 (中文化 README)
Task 5 (AGENTS.md) → 最后，因为引用已经确定的新变量名和版本号
```

## 四、验证

- [ ] `grep -r "MIMO_BASE_URL\|XIAOMI_API_KEY" --include="*.py" --include="*.yaml" --include="*.md" --include="*.toml" .` 返回 0（除 .git 目录和历史 plans）
- [ ] `grep "LLM_BASE_URL\|LLM_API_KEY" .env.example configs/providers.yaml scripts/healthcheck.py` 确认新变量名到位
- [ ] `python3 -m pytest tests/ -v` — 66 tests 全部通过（test_env_utils.py 变量名已更新）
- [ ] `pyproject.toml` 中 version = "1.0.1"
- [ ] README.md 以中文为主要语言
- [ ] README.en.md 存在且内容完整
- [ ] AGENTS.md 存在且包含完整部署步骤
- [ ] 本地 `.env` 变量名已更新
- [ ] `git diff --stat` 确认所有改动文件

## 五、风险

- **Skill 更新**: `paper-compass-dev` skill 中有大量 MIMO 引用，需同步更新（通过 skill_manage patch）
- **环境变量断连**: 本地 `.env` 改名后，所有调用 `LLM_BASE_URL` 的新代码会自然生效（因为 env_utils.py 无条件覆盖）
- **测试兼容**: `test_env_utils.py` 直接引用 `MIMO_BASE_URL`，改后必须测试通过
